---


<ParamField header="X-API-Key" type="string" required>
  Your unique API key for authentication.
</ParamField>
title: 'Library Index'
description: 'Retrieve all synced HTML designs.'
api: 'GET /templates'
---

### Query Parameters

<ParamField query="page" type="integer">
  Page number
</ParamField>

<ParamField query="search" type="string">
  Search in name or content
</ParamField>

<ResponseExample>

```json Response
{
  "success": true,
  "data": {
    "templates": [
      {
        "id": "441b-99ef",
        "name": "Midnight Minimal"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 1,
      "pages": 1
    }
  }
}
```

</ResponseExample>
