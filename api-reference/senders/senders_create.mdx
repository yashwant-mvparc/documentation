---


<ParamField header="X-API-Key" type="string" required>
  Your unique API key for authentication.
</ParamField>
title: 'Provision New Sender'
description: 'Initiate DNS/Email verification for a new identity.'
api: 'POST /sender-emails'
---

### Request Body

<ParamField body="name" type="string" required>
  Internal label for the sender
</ParamField>

<ParamField body="email" type="string" required>
  The 'from' email address
</ParamField>

<ParamField body="provider" type="string" required>
  Provider type (aws_ses, gmail_smtp, wildduck, email_infrastructure, smtp)
</ParamField>

<ParamField body="from_name" type="string">
  Display name in recipients' inbox
</ParamField>

<ParamField body="reply_to_email" type="string">
  Address for replies
</ParamField>

<ParamField body="verified_domain_id" type="string">
  Required for 'email_infrastructure' and shared SES
</ParamField>

<ParamField body="smtp_host" type="string">
  SMTP host if provider is 'smtp'
</ParamField>

<ParamField body="smtp_port" type="integer">
  SMTP port
</ParamField>

<ParamField body="smtp_username" type="string">
  SMTP username
</ParamField>

<ParamField body="smtp_password" type="string">
  SMTP password
</ParamField>

<ParamField body="gmail_app_password" type="string">
  Gmail app password if using gmail_smtp
</ParamField>

<RequestExample>

```json Request
{
  "name": "Global Support Desk",
  "email": "support@yourdomain.com",
  "provider": "aws_ses",
  "from_name": "Syntr Support",
  "reply_to_email": "tickets@yourdomain.com"
}
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true,
  "data": {
    "id": "b7e1-acde",
    "tracking_id": "verify_seq_8872"
  }
}
```

</ResponseExample>
